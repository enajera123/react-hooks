useEffect(() => {
    if (!isRunning) return

    const interval = setInterval(() => {
        setSeconds(s => s + 1)
    }, 1000)

    // Función de limpieza
    return () => clearInterval(interval)
}, [isRunning]) // Se ejecuta cuando isRunning cambia